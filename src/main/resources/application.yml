spring:
  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://db:3306/board?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: lvalentine
    password: lvalentine

security:
  jwt:
    token:
      secret-key: testSecretKey

logging:
  level:
    root: info
